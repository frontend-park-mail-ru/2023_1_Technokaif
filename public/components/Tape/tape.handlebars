<div class="{{tapeDiv}}">
    <div class="{{titleMainDivClass}}">
        <div class="{{titleOfTrackClass}}">
            <h2>{{titleText}}</h2>
        </div>

        <div class="{{fullListClass}}">
            <p>{{fullListText}}</p>
        </div>
    </div>

    <div class="{{contentDivClass}}">
        {{#each content.items}}
            {{> ../Cover/cover.handlebars}}
        {{/each}}
    </div>
</div>
